//This program is meant to find the duplicated number in an array where every number WILL be less than or equal to size of the array
//Example: [1, 2, 3, 4] is valid but [1,2,4,0] is missing 3 so our program will return 3;

class Solution {
    public boolean containsDuplicate(int[] nums) {
        Arrays.sort(nums);
        for(int i = 0; i < nums.length - 1; i++){
            if(nums[i] == nums[i+1]) return true;
        }
        return false;
    }
} 
